x = x + 1.5;

if (place_meeting(x,y - 1,oPlayer)) 
{
	oPlayer.x = oPlayer.x + 1.5;
}
if (place_meeting(x - 1, y, oWall2))
{
	x = xstart
}
if (devmode = false) {
if (place_meeting(x - 1, y, oPlayer) or (place_meeting(x - 1, y, oPlayer)))
{
	oPlayer.x = oPlayer.x + 1.5;
}
}